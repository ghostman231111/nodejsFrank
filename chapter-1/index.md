## What is semVer? How "~", "^" and "*" works in package.json?

## How does queryString.parse() method works? 
- // "http://mysite.com/?account=36764&add=125";
- let parsedURL = url.parse(request.url);
- let parsedParams = querystring.parse(parsedURL);
- Write the output
## What does queryString.stringify return on above output?
